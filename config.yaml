account_number: "402861090"
display_timezone: "America/Los_Angeles"  # IANA timezone for dashboard display

watchlist:
  - GOOGL
  - AAPL
  - NVDA
  - AVGO
  - CRWV
  - XLE

strategy:
  rsi_period: 14
  oversold: 30
  overbought: 70
  bar_interval: "10minute"
  lookback_days: 7
  macd_fast: 12
  macd_slow: 26
  macd_signal_period: 9
  bb_period: 20
  bb_std_dev: 2.0

risk:
  max_trade_usd: 300.0
  max_positions: 5
  daily_loss_limit_usd: 50.0
  # Order-level safety guardrails (RiskLimits)
  max_notional_per_order: 300.0    # hard cap per single order, never > $1,000
  max_orders_per_cycle: 3          # max orders placed in one 15-min reconcile cycle
  max_concentration_pct: 25.0      # max % of portfolio in a single symbol per order

tax:
  short_term_rate: 0.24
  long_term_rate: 0.15

broker:
  mcp_url: "https://agent.robinhood.com/mcp/trading"
  auth_url: "https://robinhood.com/oauth"
  token_url: "https://api.robinhood.com/oauth2/token/"
  redirect_uri: "http://localhost:3118/callback"
  scope: "internal"
  resource: "https://agent.robinhood.com/mcp/trading"
